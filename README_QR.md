# Thư mục QR

Web đang hiển thị QR ở đầu trang bằng đường dẫn:

```text
doc_QR/QR.JPG
```

Muốn thay QR khác, chỉ cần thay đúng file `doc_QR/QR.JPG`, giữ nguyên tên file và định dạng `.JPG`.

Lưu ý: Không cần sửa các file âm thanh trong thư mục `34tinh`.
